import extend from 'smart-extend'
import template, * as templates from '../choice/template'

export default template.extend()
export choice = templates.choice.extend()
export choiceIcon = templates.choiceIcon.extend()
export choiceGroup = templates.choiceGroup.extend()